<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.gatekeeperx.devicex.foodhub">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name="com.gatekeeperx.devicex.foodhub.DemoApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FoodHub">
        <activity
            android:name="com.gatekeeperx.devicex.foodhub.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.FoodHub">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.gatekeeperx.devicex.foodhub.DishDetailsActivity"
            android:exported="false"
            android:theme="@style/Theme.FoodHub" />

        <activity
            android:name="com.gatekeeperx.devicex.foodhub.CheckoutCartActivity"
            android:exported="false"
            android:theme="@style/Theme.FoodHub" />

        <activity
            android:name="com.gatekeeperx.devicex.foodhub.OrderTrackingActivity"
            android:exported="false"
            android:theme="@style/Theme.FoodHub" />
    </application>

</manifest>